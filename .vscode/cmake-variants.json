{
  "buildType": {
    "default": "debug",
    "description": "Build type",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Debug build",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Release build",
        "buildType": "Release"
      }
    }
  },
  "fallback": {
    "default": "disabled",
    "description": "Reference build based on the original source code",
    "choices": {
      "enabled": {
        "short": "Reference",
        "long": "Use an original source code",
        "settings": {
          "RC_FALLBACK": "ON"
        }
      },
      "disabled": {
        "short": "Migration",
        "long": "Use new code migrated on Rust",
        "settings": {
          "RC_FALLBACK": "OFF"
        }
      }
    }
  }
}