{
  "buildType": {
    "default": "debug",
    "description": "Build type",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Debug build",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Release build",
        "buildType": "Release"
      }
    }
  },
  "legacy_build": {
    "default": "enabled",
    "description": "Legacy build based on the original source code",
    "choices": {
      "enabled": {
        "short": "Legacy",
        "long": "Use an original source code",
        "settings": {
          "LEGACY_BUILD": "ON"
        }
      },
      "disabled": {
        "short": "Migration",
        "long": "Use new code migrated on Rust",
        "settings": {
          "LEGACY_BUILD": "OFF"
        }
      }
    }
  }
}
